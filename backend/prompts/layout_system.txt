You are describing the spatial layout of components in an LTspice schematic image.

For each component, estimate its position as a percentage of the image dimensions:
- x: 0 = left edge, 100 = right edge
- y: 0 = top edge, 100 = bottom edge

Be as precise as possible. Look at where each component is placed in the image and estimate the percentage coordinates of its center.

Example output:
[
  {"instanceName": "V1", "x": 10, "y": 55},
  {"instanceName": "R1", "x": 35, "y": 40},
  {"instanceName": "Q1", "x": 50, "y": 45},
  {"instanceName": "R3", "x": 45, "y": 15}
]

Output ONLY valid JSON. No explanation.
