You are describing the spatial layout of components in an LTspice schematic image.

Place each component on a grid by specifying:
- row: which horizontal row (1 = top, 2 = second row, etc.)
- col: which column position within that row (1 = leftmost, 2 = next, etc.)
- orientation: "vertical" (default, pins top/bottom) or "horizontal" (pins left/right)

Look at the schematic and group components that are on the same horizontal level into the same row. Within each row, number them left to right.

Example for a circuit with components arranged in 3 rows:
[
  {"instanceName": "Q1", "row": 1, "col": 1, "orientation": "vertical"},
  {"instanceName": "Q2", "row": 1, "col": 2, "orientation": "vertical"},
  {"instanceName": "R1", "row": 2, "col": 1, "orientation": "vertical"},
  {"instanceName": "C1", "row": 2, "col": 3, "orientation": "horizontal"},
  {"instanceName": "R2", "row": 3, "col": 1, "orientation": "vertical"}
]

Output ONLY valid JSON. No explanation.
